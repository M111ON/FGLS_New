# HANDOFF — Frustum / Fabric Wire Pipeline
_Session: 2026-05-09_

---

## Tasks Completed This Session

| Task | File | Tests |
|------|------|-------|
| #1 drain_gap wire | `fabric_wire_drain.h` | ✅ 38/38 |
| #2 pentagon drain offset | `test_pentagon_drain.c` | ✅ 12/12 |
| #3 1440 migration overlay | `pogls_1440.h` | ✅ 14/14 |

**Total: 64 tests PASS, 0 FAIL**

---

## Locked Constants

```c
/* fabric_wire.h */
POGLS_TRING_FACES    = 12
POGLS_SACRED_NEXUS   = 54
POGLS_FILE_SIZE      = 4896   /* 17×2×144 */
POGLS_LANE_GROUPS    = 6
POGLS_DRAIN_COSET_SIZE = 408  /* 4896/12 */

/* pogls_1440.h */
POGLS_BASE_CYCLE     = 720
POGLS_EXT_CYCLE      = 1440   /* 2×720 */
```

## Locked Behaviors

- `drain_gap = (trit^slope)&1` → drain_state[] in FrustumBlock
- `drain_id = trit % 12` → `pogls_pentagon_drain_offset(drain_id)`
- `phase = idx / 720` XOR overlay — core 720 untouched
- header = logical overlay บน `reserved[0..16]`, no extra bytes
- `SHADOW_BIT_FENCE` irreversible once set (heptagon one-way valve)

---

## Remaining Tasks

| Task | Spec |
|------|------|
| #4 rotation field | `rotation_state` wrap at 6, `fabric_rotation_advance()` มีแล้ว — test coverage + wire to 1440 phase? |
| #5 heptagon fence | full fence path test: CORE→shadow write, shadow→flush read, deny cases |
| #6 atomic reshape | `pogls_reshape_ready(54)` trigger → full sequence: collect→stage→flip→drain |

---

## Open Prompt (Next Session)

```
POGLS session handoff 2026-05-09.

Locked: 
  Task #1 drain_gap=(trit^slope)&1 → fabric_wire_drain.h (38/38)
  Task #2 pentagon drain offset 12×408B=4896 (12/12)
  Task #3 1440 overlay phase XOR, core 720 untouched (14/14)

Files: frustum_layout_v2.h, fabric_wire.h, fabric_wire_drain.h, pogls_1440.h

Next: Task #4 — rotation_state field
  fabric_rotation_advance() มีแล้ว (wraps at 6)
  ต้องการ spec ว่า Task #4 = test coverage only หรือ wire เพิ่ม?
```
