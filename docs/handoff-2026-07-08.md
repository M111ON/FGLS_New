# FGLS Session Handoff — 2026-07-08

## Goal of Next Session
Continue SID/DRamTile/VRamTile pipeline — fix remaining bugs, integrate with GPU, or start new work area.

## State of Play

### Done This Session
- **DGFS Drive**: WinFsp FUSE mount X:\ backed by DRamTile store. Fix: `dt_free` before `dirlist` resize (root cause of write failures). `statfs` block count overflow fix. Delayed write fix (zero-size entry on create). Read speed ~2000 MB/s.
- **dgfs-launcher.ps1**: PowerShell GUI for Drive Mode vs LLM pass-through.
- **SID+GPU DeviceLost fix**: `tensor_update_data()` uses `ggml_backend_tensor_set()` for GPU tensors instead of pointer swap.
- **SID restore GPU crash fix**: restore uses `delta_orig_data` (CPU DRamTile copy) not `found_tensors[fi].orig_data` (GPU device pointer).
- **DRamTile benchmark** (LFM2.5-8B-A1B-Q4_K_M): CPU -36%, GPU +38%, 5-face -23%.
- **Pipe ABI** (collection/pipe/): pipe_abi.h, pipe_backend.h, pipe_context.h + test suite.
- **Commit**: `df59689` on sid-runner branch.

### Known Issues
- `-O2` strict aliasing UB in tensor memory scanner → crash in optimized build (use `-O0` or `-fno-strict-aliasing`).
- `--twin-gpu` uses simulated memcpy, not real `cudaMemcpy` via icosa_bridge.dll.
- DGFS Drive write has FUSE overhead (~5ms/1KB write).

## Open Decisions
1. DGFS Drive: keep as optional feature? (Used now but user may not need it).
2. VRamTile Gear 2: direct GPU buffer write via saved Vulkan offset — nice-to-have.
3. Next focus area: `-O2` fix, `--twin-gpu` wiring, or TTS/VLM integration (card #42 on board).

## Skills to Use
- `msys2-build-pipeline` — building/linking on Windows with MinGW
- `cross-session-board` — post progress cards
- `focused-fix` — for systematic deep-dive on remaining bugs

## Artifacts (not duplicated here)
- `AGENTS.md` — full session history, benchmark table, all milestones
- Commit `df59689` — DGFS Drive + DRamTile free-list + Pipe ABI
- `I:\DGLS\dgfs_drive.c` — DGFS WinFsp FUSE implementation
- `I:\DGLS\dgfs-launcher.ps1` — PowerShell GUI launcher
- `runner/vramtile.h` — VRamTile GPU cache layer
- `runner/dramtile_store.h` — DRamTile store engine
- `runner/llama_pogls_runner_sid_v2.c` — main runner with SID+GPU integration
- `runner/bench_sid.ps1` — benchmark script
- `I:\model\LFM2.5-8B-A1B-Q4_K_M.gguf` — test model
