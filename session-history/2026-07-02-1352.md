# Session 2026-07-02 13:52

**Intent**: FEATURE_BUILDING (runner-up: EXPLORING)
**Signal**: HIGH
**Frames active**: A (0.8) / B (0.5) / C (0.6) / D (0.8)

## What shipped
- **Inbox MCP server** (`inbox_mcp_server.py`): Added workspace auto-detection (CWD/--workspace arg), web dashboard with `/api/state`, `/api/handoff`, and `/` (HTML) endpoints via Python `http.server`
- **Inbox dashboard** (`inbox_dashboard.html`): Auto-refreshing web UI with stats bar, board cards, sources list, handoff summary
- **Global installation**: Copied MCP server + dashboard to `~/.config/opencode/tools/`, updated global `opencode.jsonc`, removed local `opencode.jsonc` override
- **Cross-session-board skill**: Created `SKILL.md` + `CLAUDE.md` with New Session Protocol at `~/.config/opencode/skills/`
- **AGENTS.md**: Updated with cross-session board section referencing global skill
- **Hardlink dedup**: 3 identical `engine_dashboard.py` copies → 1 canonical + 2 hardlinks

## What was figured out
- MCP server can't serve board data directly through MCP protocol for web consumption → needed separate HTTP server alongside
- Dashboard JS initially called wrong API endpoint (`/api/tool` POST) when server only had GET endpoints → had to rewrite JS to use `/api/state` and `/api/handoff` directly
- Global skill + config approach: project-level `opencode.jsonc` override without inbox-manager → inherits from global config → cleaner per-workspace behavior

## Decisions made this session
- Web dashboard over terminal/MCP/AI-display (user chose "web or any interface")
- `http.server` over Flask (zero external dependencies)
- Hardlinks over symlinks for dedup
- Extend existing MCP server vs create new board-only server (1 config entry simpler)

## Where it got hard
- Dashboard API error: JS called `/api/tool` (POST) but server only had GET handlers for `/api/state` and `/api/handoff` → fixed by rewriting the `loadAll()` function to use direct GET calls
- Workspace detection: needed to work during import (MCP protocol startup) before `__main__` runs → solved with module-level argparse

## AI contribution summary
Claude proposed and implemented the web dashboard approach, designed the HTML/CSS/JS template, identified the API endpoint mismatch bug, and implemented the workspace auto-detect mechanism. The user provided the requirement direction ("click to see details," "any interface") and confirmed the global skill structure approach.

## Next steps inferred
- Consider adding WebSocket support for live updates instead of polling
- Add more dashboard features: incoming file browser, vault history, conflict list
- The skill `cross-session-board` needs verification in a fresh opencode session
