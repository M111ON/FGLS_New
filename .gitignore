# ── Compiled / binary ──
*.o
*.obj
*.out
*.exe
*.dll
!collection/geo_jump_module/libgeojump.dll
*.so
*.bin

# ── Python ──
*.pyc
__pycache__/

# ── Vault / snapshots (inbox-manager) ──
*.bak
#*.snap
**/.vault/

# ── Runtime logs ──
*.log

# ── Model weight stores (huge — 100s of MB each) ──
*.gsten
*.gsdat
*.gsidx
*.qdat
*.pwc
*.f32
*.shape
*.npz
*.pogls
*.rad

# ── Build / temp output ──
build/
Temp/
/open-design

# ── Node / SDK cache ──
node_modules/
.opencode/

# ── Archives ──
*.zip
*.7z
*.tar.gz
collection.zip
bak_collection.zip

# ── Error logs / temp output ──
*_err.txt
*_out.txt
*_pp.txt
*stderr*
*stdout*
temp_jsonc.json
output/_prose_block.txt

# ── Pipeline linker output ──
*.exp
*.lib

# ── Compiled binaries (no extension) ──
runner/addr_resolve
runner/dramtile_dump
runner/pogls_build
runner/pogls_cat
runner/pogls_compress
runner/pogls_decompress
runner/pogls_diff
runner/pogls_inspect
runner/pogls_roundtrip
runner/pogls_test
runner/pogls_verify
runner/test.bpack

# ── Node / editor configs (local only) ──
package.json
package-lock.json
opencode.json
xopencode.json
magic-context.jsonc.*
cross-env
hermes@*

# ── Old test data / temp dirs ──
pipeline/test_data/
pipeline/test_tmp/

# ── Stray files from bad commands ──
--help
-f
runner/nul

# ── Model weight stores (huge) ──
*.dramtile
models/
test_model.dramtile
real_weights.dramtile

# ── Archives (zip/ = old snapshots) ──
zip/

# ── Harness memory cache ──
# (opencode memory — tracked for cross-session persistence)
# .harness-memory/

# ── Generated images / docs ──
*.bmp
*.webp
*.jpg
*.jpeg
*.png
*.svg
*.pdf

# ── Memory store ──
memory_store/raw/

# ── Inbox state ──
.inbox_state.json
.inbox_index.md
*.bak
.protect_alreadyrun.ps1

/.playwright-mcp
/runner/ses_profiles
/runner/test_tail
/runner/cpl_profiles
/.file_vault
runner/nul
clangd-mcp-server/
*.gfuf
*.geopixel
*.gpf
*.gpxl
*.dec
*.gpx5
.hermes/

# ── Generated / upload junk ──
uploads/
query
model.fgls
